import { ButtonDemoDirective } from './button-demo.directive';

describe('ButtonDemoDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonDemoDirective();
    expect(directive).toBeTruthy();
  });
});
