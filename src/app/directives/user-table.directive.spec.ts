import { UserTableDirective } from './user-table.directive';

describe('UserTableDirective', () => {
  it('should create an instance', () => {
    const directive = new UserTableDirective();
    expect(directive).toBeTruthy();
  });
});
